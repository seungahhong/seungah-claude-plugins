{
  "description": "meta-harness self-heal capture. 사용자의 '수정/보강/방향전환(redirect)' 발화를 UserPromptSubmit에서 감지해 experience-store의 signals 레인에 원형으로 적재한다(캡처 전용·비차단). 적재된 신호는 추후 /meta-harness(healer)가 causal 진단·Pareto 비후퇴 패치에 사용한다 — 적용은 사용자 승인 게이트 통과 후에만.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/self-heal-capture.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
