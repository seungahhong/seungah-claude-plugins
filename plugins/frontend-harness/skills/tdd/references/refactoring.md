# 리팩토링 후보

TDD 사이클 후 찾아야 할 것들:

- **중복** → 함수/클래스 추출
- **긴 메서드** → 비공개 헬퍼로 분리 (테스트는 공개 인터페이스에 유지)
- **얕은 모듈** → 결합하거나 심화
- **기능 부러움(Feature Envy)** → 데이터가 있는 곳으로 로직 이동
- **원시 타입 집착** → 값 객체 도입
- **새 코드가 드러낸 기존 코드의 문제** → 기존 코드도 개선
